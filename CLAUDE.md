# Vim 学习站(macOS · VSCodeVim + IdeaVim 教程站点)

**开局三步(每次会话,含上下文被清理后):**

1. 读 `STATUS.md` — 进度唯一真相,按「总览 → 下一步任务」行事。
2. 需要规范时读 `PLAN.md`:章节大纲 §4 / 内容模板 §5 / 演示 JSON Schema §6 / 风格 §7 / 验收 §9。
3. 完成任何任务:立即更新 `STATUS.md` 并跑 `node tools/check.mjs`。

规则:聊天记录不是记忆;`done` 的章节冻结不重写;纯静态零构建、零 CDN 依赖;不主动 git commit。
